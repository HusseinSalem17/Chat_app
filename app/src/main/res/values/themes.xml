<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.ChatMe" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!--        we will use the same theme for both normal mode and dark mode so just copy and paste
                    the whole style tag to the themes file for nigh mode to make both themes similar.-->
        <item name="colorPrimary">@color/primary</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/primary</item>
    </style>
</resources>